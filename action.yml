# action.yml
name: 'deb-checker'
description: 'Test build of a debian package with additional checks'
inputs:
  err-on-lint:
    description: 'fail on lintian warnings'
    required: false
    default: 'false'
outputs:
  debversion:
    description: 'debian package version'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.err-on-lint }}
